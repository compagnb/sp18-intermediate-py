# boxwocorners

for i in range(0,4):
            t.penup()
            t.forward(20)
            t.pendown()
            t.forward(60)
            t.penup()
            t.forward(20)
            t.right(90)